# Academic Website Project Instructions

## Project Root

- This folder is the real GitHub Pages repository for `jorgepascoa.github.io`.
- Treat `C:\Users\jpjor\Documents\Research\academic-website\academicpages-site` as the project root.
- The parent folder may contain scratch or archived files from earlier Codex chats; do not edit those unless explicitly asked.

## Website Stack

- This is an Academic Pages / Jekyll site.
- Preserve GitHub Pages conventions, including `_config.yml`, `_pages`, `_publications`, `_talks`, `_teaching`, `_data`, `_layouts`, `_includes`, and `images`.
- Keep `baseurl` empty for the user site `https://jorgepascoa.github.io`.
- Keep repository metadata aligned with `jorgepascoa/jorgepascoa.github.io`.

## Editing Rules

- Before editing, check `git status` and explain what files are likely to change.
- Prefer small, focused edits that preserve the existing theme structure.
- Do not overwrite academic content, CV links, profile details, publication entries, talks, or teaching entries without explicit approval.
- Do not remove Google verification, robots.txt, or GitHub Pages configuration files unless explicitly asked.
- If changing layout or CSS, check both desktop and mobile behavior when feasible.

## Git And Publishing

- The remote should remain `https://github.com/jorgepascoa/jorgepascoa.github.io.git`.
- Use branch `main` unless the user asks otherwise.
- Before pushing or publishing, summarize the diff and ask for approval.
- Do not run destructive Git commands unless explicitly requested.

## Content Style

- Use a professional academic tone.
- Prefer concise profile text suitable for an economics PhD student.
- Avoid inventing publications, awards, affiliations, courses, talks, or coauthors.
- Preserve accents in names where the files already use them correctly.
